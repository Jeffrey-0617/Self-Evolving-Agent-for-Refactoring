# Skill Summary

| skill name | kind | scope | description | linked_patterns | status |
|---|---|---|---|---|---|
| refactoring-workflow | workflow | wright-adl | Wright# ADL refactoring workflow — generate assertions (all property types), produce refactored ADL, apply static analysis tools, verify, fix with a capped verification loop, and write structured log. | — | active |
| post-reflection | workflow | memory | After a refactoring task, distill tmp/log.md into .claude/MEMORY (patterns and episodes), generalize into reusable rules, and create/update SKILLs; update the index. | — | active |
| retrieval | workflow | memory | Load relevant patterns from .claude/MEMORY for the current task (e.g. at task start). | — | active |
| distill | workflow | memory | Record an analysis-mistake pattern when a rule was misread during tool application (mid-task). Distill the mistake into .claude/MEMORY. | — | active |
| wrighthash-tool-gen | workflow | tool-dev | Creates or updates Wright# ADL static analysis tools for constraint rules: add test cases, implement/fix the tool, ensure all tests pass, then draft/update the tool SKILL.md. | — | active |
| wrighthash-tool-apply | workflow | tool-dev | Applies Wright# ADL static analysis tools to tmp/refactored.adl, cross-checks results with independent manual analysis, resolves any discrepancy, updates skill docs with new failure cases. | — | active |
| check-assertion-events | tool | assertion-authoring | Checks Wright# ADL for assertion event name mismatches — verifies every Component.port.event reference in assert statements exists as a declared event in the corresponding port's CSP expression. | assertion-event-name-mismatch | active |
| reusableskill-assertion-design | reusable-rule | assertion-design | Reusable rules for writing correct temporal-logic assertions in Wright# (CSP-based) ADL systems; covers liveness vs. until-formula selection, event name verification, concurrent cycle semantics, unsupported conditional-absence syntax, and multi-hop liveness failure across one-shot connector `<*>` paths. | until-assertion-csp-anti-pattern, assertion-event-name-mismatch, conditional-absence-assertion-unsupported, one-shot-multihop-liveness-invalid | active |
| reusableskill-connector-rules | reusable-rule | attach-design | Reusable design rules for Wright# connectors and multi-role port attachments; covers input/output role identification, Rule 6 attach ordering, port isolation for multiple input roles, PSConnector fan-out topology (one input, N outputs), backend decomposition topology (frontend fan-out, port isolation per backend, fan-out extension on existing services, wire reuse), IOConnector streaming push topology (extsupplier=recurring output, blockstorage=recurring input, one wire per consumer), API Gateway frontend migration topology (remove direct wires, route via gateway, BFF aggregator, CDN custom connector), plugin/technology retirement topology (Bridge as CHAINConnector forwarder, middleman promotion, receiver-slot re-attachment), and mediator/bus port extension topology (receiver-then-forwarder on existing port, new dedicated ports on existing mediator for new lanes). | port-isolation-for-dual-input-roles, psconnector-fanout-multi-role-topology, backend-split-decomposition-topology, ioconnector-streaming-push-topology, api-gateway-frontend-migration-topology, plugin-retirement-bridge-middleman-topology, mediator-bus-port-extension-topology | active |
| check-port-name-uniqueness | tool | port-naming | Checks Wright# ADL for duplicate port names across components — verifies that every port name declared in any component is globally unique across all components (Rule 1 enforcement); invoke when verifying a refactored ADL. | duplicate-port-name-across-components | active |
| check-adl-no-asserts | tool | verification-workflow | Checks that a Wright# ADL file contains no embedded assert statements — asserts must be placed only in tmp/assertions.md, not in the ADL file, to prevent PAT errors during partial-system verification; invoke as the first static analysis step. | adl-file-no-assert-statements | active |
| check-role-param-integers | tool | connector-role-attach | Checks Wright# ADL attach statements for non-integer connector role parameters — every parameterised role (publisher, requester, writer, reader, extsupplier, querier, forwarder) must use a unique integer literal; letter identifiers cause PAT to return "An error has occurred." for all partial systems. | connector-role-parameter-must-be-integer | active |
| reusableskill-verification-diagnostics | reusable-rule | verification-workflow | Reusable diagnostic rules for interpreting Wright# verifier outcomes; covers distinguishing server-unavailability INVALID (HTTP connection errors) from genuine assertion INVALID, deferring to static analysis tools when the PAT server is unreachable, and structural deadlock INVALID from one-shot connector `<*>` attachments (not fixable by assertion changes). | verifier-connection-error-as-invalid, one-shot-multihop-liveness-invalid | active |
